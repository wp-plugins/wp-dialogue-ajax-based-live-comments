// js/app.js
"use strict";
var app = app || {};
var ENTER_KEY = 13;

$(function() {

    // Kick things off by creating the **App**.
    var commentView = new app.CommentView(app_vars);

});